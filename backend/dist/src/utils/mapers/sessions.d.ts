import SessionEntity from 'src/modules/session/session.enity';
import { PublicSession } from 'src/modules/session/session.types';
export declare const getPublicSession: (session: SessionEntity) => PublicSession;
