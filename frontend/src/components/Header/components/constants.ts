const Settings = {
    'SESSIONS': "My session", 'PROFILE': "Profile", 'LOGOUT': "Logout"
}

const AvatarMenuOptions = [Settings.SESSIONS, Settings.PROFILE, Settings.LOGOUT];

export { Settings, AvatarMenuOptions }