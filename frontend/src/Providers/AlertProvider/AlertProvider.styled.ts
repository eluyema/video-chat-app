import Alert from '@mui/material/Alert';
import { styled } from "@mui/material";

const AlertWrapper = styled('div')({
    position: "fixed",
    width: '40%',
    right: '10px',
    bottom: "10px",
    zIndex: '10'
})

export { AlertWrapper };
